import Root from './Root'
import Header from './Header'
import Body from './Body'
import Footer from './Footer'

const RulesContainer = Object.assign(Root, { Header, Body, Footer })

export default RulesContainer
